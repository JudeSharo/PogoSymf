{% extends 'base.html.twig' %} 
{% block title %}
	L'event n°{{event.id}}
{% endblock %}
{% block body %}
	<article>	
		<h1>{{event.title}}</h1>
		{% if event.image == "pogo4" %}
			<img src="{{asset('image/pogo4.jpg')}}">
		{% endif %}
		<p>{{event.description}}</p>
	</article>

{% endblock %}
